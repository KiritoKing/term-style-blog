# Tasks

- [x] 1. Add B02 registry and handoff plan before implementation.
- [x] 2. Copy the accepted immutable deployment workflow, ESM validator and Node tests from staging.
- [x] 3. Add `test:deployment` and invoke it from offline CI and the deployment workflow.
- [x] 4. Verify snapshot and input validation preserves both `publish` and `published` while rejecting draft, secret and local-media scope.
- [x] 5. Verify preview `noindex` output cannot pass production site validation.
- [x] 6. Parse workflows as YAML and syntax-check every Bash run block.
- [x] 7. Check pinned actions, read-only permissions, no secret output and no dangling scripts.
- [x] 8. Run strict B02 OpenSpec validation and diff check without rebuilding `dist`.
- [x] 9. Write B02 report/handoff, move the task to review and freeze files for supervisor acceptance.
