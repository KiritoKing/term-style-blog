Use Vercel’s supported git.deploymentEnabled=false opt-out, not a new deployment service or a deleted project. Keep CI on PR and main push: protected-branch status checks must still resolve for documentation-only changes, and main validates the merge result. Existing concurrency cancels superseded branch runs. CodeQL remains GitHub-managed with its existing languages and schedule. Group npm minor/patch updates and GitHub Actions updates weekly (Monday09:00 Asia/Shanghai), with separate security-update grouping; no auto-merge. Preserve immutable content publication inputs, owner/main guard and false/manual production policy. The live-pipeline reviewer only reads Hermes services/config; root alone changes repository files.
