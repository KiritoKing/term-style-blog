Validate JSON and YAML plus embedded shell. Re-run the existing deployment/workflow boundary regression and strict OpenSpec checks. On the exact PR commit, require existing CI and CodeQL to pass and inspect GitHub check contexts to confirm Vercel stopped creating deployments. Independently read Hermes units, timers, exporter policy and GitHub publication state before documenting trigger cadence. No new product tests are needed for reversible naming/scheduling/opt-out configuration.
